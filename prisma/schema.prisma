generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "cockroachdb"
  url      = env("DATABASE_URL")

}

model  Messages {
  id    String @id @default(uuid())
  text  String
  like  Int    @default(0)
}